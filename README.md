use the html5 download attribute of the anchor element and the data: scheme to save form data to the local filesystem as JSON

here's a jquery version
tested on chrome
dependencies: html5

i'll post a vanilla javascript version later
